import type { ReactNode } from "react";

/** Passthrough — Explore SEO metadata is query-aware on page.tsx. */
export default function ListingsLayout({ children }: { children: ReactNode }) {
  return children;
}
