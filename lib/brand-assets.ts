/**
 * Nexa Stays brand assets.
 * Source artwork: `images/nexastays.png` at repo root (design / reference).
 * Served URL: `public/images/nexastays.png` → keep both in sync when updating the logo.
 */
export const NEXA_STAYS_LOGO_SRC = "/images/nexastays.png" as const;
