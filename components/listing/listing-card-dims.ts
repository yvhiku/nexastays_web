/** Shared ListingCard layout tokens — skeletons import these for zero CLS. */
export const LISTING_CARD_IMAGE_RATIO = "4/3" as const;
/** Split-pane marketplace density — shorter than default 4/3. */
export const LISTING_CARD_IMAGE_RATIO_COMPACT = "5/4" as const;
export const LISTING_CARD_RADIUS = "rounded-2xl" as const;
